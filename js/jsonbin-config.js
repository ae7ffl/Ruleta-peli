// PASO IMPORTANTE: rellena esto con los datos de TU bin de jsonbin.io.
// Están explicados paso a paso en el README.md ("1. Configurar jsonbin.io").

const JSONBIN_CONFIG = {
  binId: "TU_BIN_ID_AQUI",       // lo ves en la URL del bin en tu dashboard
  masterKey: "TU_MASTER_KEY_AQUI" // Account → API Keys → tu X-Master-Key
};
